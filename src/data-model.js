class Data_Model {
  constructor(name, schedule, meeting_link, video_link, logo_url) {
    this.name = name;
    this.schedule = schedule;
    this.meeting_link = meeting_link;
    this.video_link = video_link;
    this.logo_url = logo_url;
  }
}

export { Data_Model };
